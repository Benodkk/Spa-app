import { Container } from "@mui/material";
import { styled } from "@mui/system";

export const StyledStatusInfo = styled(Container)({
  display: "flex",
  alignItems: "center",
  flexDirection: "column",
});
