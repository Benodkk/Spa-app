import React from "react";
import { StyledStatusInfo } from "../style/StatusInfo.style";

const Error = () => {
  return (
    <StyledStatusInfo>
      <div>Error! Can not fetch the data!</div>
    </StyledStatusInfo>
  );
};

export default Error;
